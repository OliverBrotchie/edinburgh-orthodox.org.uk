---
interface Props {
    name: string;
    address: string;
    href: string;
}

const { name, address, href } = Astro.props;
---

<a class="church" href={href} target="_blank" rel="noopener">
    <h3>{name}</h3>
    <p class="muted">{address}</p>
</a>
